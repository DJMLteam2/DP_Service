<!-- detailHeader.mustache -->
<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">

    <style>
        /* 전체 페이지에 대한 일반적인 스타일 */
        .wrapper {
            width: auto;
            height: auto;
            max-width: 100%;
            margin: auto;
            padding: 10px;
        }
        body {
            font-family: 'Montserrat';
            background: #e7e8f3;
            margin: 0;
            padding: 0;
        }
    
        /* 메인 제품 컨테이너에 대한 스타일 */
        .product {
            position: relative;
            margin: auto;
            width: 100%; /* 전체 너비 */
            max-width: 950px; /* 최대 너비 */
            height: auto;
            background: white;
            display: block;
            top: 45px;
            box-shadow: 3px 3px 70px 0px rgba(50, 50, 50, .5);
            -webkit-box-shadow: 3px 3px 70px 0px rgba(50, 50, 50, .5);
            overflow: hidden;
        }
    
        /* 왼쪽 세부 섹션에 대한 스타일 */
        .detail {
            position: relative;
            width: 50%; /* 컨테이너 너비의 절반 */
            height: auto%;
            display: inline-block;
            vertical-align: top;
        }
    
        /* 초기 세부 섹션에 대한 추가 스타일 */
        .detail-initial {
            width: 100%; /* 세부 섹션 내에서 전체 너비 */
        }
    
        /* 오른쪽 이미지 섹션에 대한 스타일 */
        .image {
            position: relative;
            width: 50%; /* 컨테이너 너비의 절반 */
            height: 100%;
            display: inline-block;
            background-size: cover; /* 이미지를 컨테이너에 맞게 자동으로 조절 */
            background-position: center; /* 이미지 중앙 정렬 */
            background-repeat: no-repeat; /* 배경 이미지 반복 제거 */
        }
        
        /* 오른쪽 이미지 세션에 대한 자식요소 */
        .image img {
            width: 100%; /* 이미지가 부모 컨테이너에 가득 차도록 설정 */
            height: 100%; /* 높이도 자동으로 조절하여 비율 유지 */
            object-fit: cover; /* 이미지의 가로 및 세로 비율 유지하면서 컨테이너에 맞춤 */
        }
    
        /* 세부 섹션 내 콘텐츠에 대한 스타일 */
        .content {
            margin: 20px 60px 0px 78px; /* 위, 오른쪽, 아래, 왼쪽 여백 */
            overflow: hidden; /* 숨겨진 오버플로우 */
        }
        .header h1 {
            margin: 0; /* 기본 마진 제거 */
            text-align: center; /* h1 요소를 가운데 정렬 */
        }
        /* 콘텐츠 내 헤더 텍스트에 대한 스타일 */
        .header span {
            font-size: 15px;
            color: #919191;
            font-weight: bold;
        }
    
        /* 콘텐츠 내 가격 텍스트에 대한 스타일 */
        .content .price {
            font-weight: bold;
            font-size: 17px;
            color: #a8a8a8;
        }
    
        /* 콘텐츠 내 설명 텍스트에 대한 스타일 */
        .content .description {
            padding: 10px 0 0 0; /* 위쪽 여백 */
            color: #a8a8a8;
            white-space: normal;
            font-size: 15px;
            font-weight: bold;
        }
    
        /* 콘텐츠 내 추가 세부 정보 텍스트에 대한 스타일 */
        .content .detail {
            color: #c7c7c7;
            font-size: 15px;
            font-weight: bold;
            margin: 10px 0 0 0; /* 위쪽 여백 */
        }
    
        /* 속성 섹션에 대한 스타일 */
        .property {
            margin: 30px 0 0 0; /* 위쪽 여백 */
        }
    
        /* 속성 아이템 내 각 항목에 대한 스타일 */
        .property .item {
            display: inline;
        }
    
        /* 속성 아이템 내 키 레이블에 대한 스타일 */
        .key {
            display: inline-block;
            min-width: 30px;
            font-size: 15px;
            font-weight: bold;
            text-align: center;
            vertical-align: top;
        }
    
        /* 속성 아이템 내 값에 대한 스타일 */
        .value {
            display: inline-block;
            white-space: normal;
            color: #a8a8a8;
            font-size: 13px;
            font-weight: bold;
            width: 33%; /* 컨테이너 너비의 1/3 */
            padding-right: 20px; /* 오른쪽 여백 */
        }
    
        /* 푸터 섹션에 대한 스타일 */
        .footer {
            padding: 30px 0px 0px 155px; /* 위, 오른쪽, 아래, 왼쪽 여백 */
        }
    
        /* 어두운 색 버튼에 대한 스타일 */
        .btn-dark {
            background: black;
            border: none;
            padding: 15px;
            color: white;
        }
        /* 블록 크기의 버튼에 대한 스타일 */
        .btn-block {
            width: 150px;
        }
    </style>
    
</head>

<body>
    <div class="header">
        <img src="../path/to/logo.png" alt="" />
    </div>
</body>
</html>
