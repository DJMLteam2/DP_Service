<!-- detailHeader.mustache -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
        integrity="sha384-r4kXmG5F5u5sk1VxE6f6HJ9jlF5Vo2FNn5c5v8w5t5tiD5MvqIHfKfj5A5f5p5tkBn"
        crossorigin="anonymous">
    <style>
        /* 전체 페이지에 대한 일반적인 스타일 */
        body {
            font-family: 'Montserrat';
            background-color: white;
            margin: 0;
            padding: 0;
        }
        /* 제목에 대한 css 적용 */
        .three h1 {
            font-size: 40px;
            font-weight: 500;
            letter-spacing: 0;
            line-height: 1.5em;
            padding-bottom: 15px;
            padding-left: 475px;
            position: relative;
        }
        
        .three h1:before,
        .three h1:after {
            content: "";
            position: absolute;
            bottom: 0;
            width: 55px;
        }
        
        .three h1:before {
            left: 475; /* Move the :before pseudo-element to the right */
            height: 5px;
            background-color: #111;
        }
        
        .three h1:after {
            left: 475px; /* Adjust the left property to move the :after pseudo-element to the right */
            height: 1px;
            width: 95%;
            max-width: 255px;
            background-color: #333;
        }
    
        /* 메인 제품 컨테이너에 대한 스타일 */
        .product {
            position: relative;
            margin: auto;
            width: 1200px; /* 전체 너비 */
            height: 750px;
            background: white;
            display: block;
            top: 30px;
            box-shadow: 3px 3px 70px 0px rgba(50, 50, 50, .5);
            -webkit-box-shadow: 3px 3px 70px 0px rgba(50, 50, 50, .5);
        }
        
        /* 왼쪽 세부 섹션에 대한 스타일 */
        .detail {
            box-sizing: border-box;
            position: relative;
            width: 50%;
            height: 100%;
            display: inline-block;
            vertical-align: top;
            border-right: 1px solid #000;
        }
        
        /* 초기 세부 섹션에 대한 추가 스타일 */
        .detail-initial {
            width: 100%; /* 세부 섹션 내에서 전체 너비 */
        }
        
        /* 오른쪽 이미지 섹션에 대한 스타일 */
        .image {
            box-sizing: border-box;
            position: relative;
            width: 50%; /* 컨테이너 너비의 절반 */
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }
        
        /* 오른쪽 이미지 세션에 대한 자식요소 */
        .image img {
            width: auto;
            height: 100%;
            max-width: 100%;
            max-height: 750px;
        }
    
        /* 세부 섹션 내 콘텐츠에 대한 스타일 */
        .content {
            margin: 20px 60px 0px 78px; /* 위, 오른쪽, 아래, 왼쪽 여백 */
            overflow: hidden; /* 숨겨진 오버플로우 */
        }
        .header h1 {
            margin: 0; /* 기본 마진 제거 */
            text-align: center; /* h1 요소를 가운데 정렬 */
            margin: 30px 60px 0px 78px;
        }
        /* 콘텐츠 내 헤더 텍스트에 대한 스타일 */
        .header span {
            font-size: 15px;
            color: #919191;
            font-weight: bold;
        }
    
        /* 콘텐츠 내 가격 텍스트에 대한 스타일 */
        .content .price {
            width: 100%;
            height: 100%;
            font-weight: bold;
            font-size: 19px;
            color: #a8a8a8;
        }
    
        /* 콘텐츠 내 설명 텍스트에 대한 스타일 */
        .content .description {
            padding: 10px 0 0 0;
            color: #a8a8a8;
            font-size: 15px;
            font-weight: bold;
            box-sizing: border-box;
        }
    
        /* 콘텐츠 내 추가 세부 정보 텍스트에 대한 스타일 */
        .content .detail {
            box-sizing: border-box;
            color: #c7c7c7;
            font-size: 15px;
            font-weight: bold;
            margin: 10px 0 0 0; /* 위쪽 여백 */
        }
    
        /* 속성 섹션에 대한 스타일 */
        .property {
            margin: 20px 0 0 0;
        }
                
        .property .item {
            display: flex;
            justify-content: space-between;
        }
        
        .key {
            display: inline-block;
            min-width: fit-content;  /* 변경 */
            font-size: 18px;
            font-weight: bold;
            text-align: left;
            vertical-align: top;
            margin-right: 0 0 0 0;
        }
        
        .value {
            display: inline-block;
            color: #a8a8a8;
            font-size: 16px;
            font-weight: bold;
            width: calc(100% - 30px);  /* 변경 */
            text-align: right;
            margin: 0 0 0 0;
        }
    
        /* 푸터 섹션에 대한 스타일 */
        .footer {
            padding: 30px 0px 0px 230px; /* 위, 오른쪽, 아래, 왼쪽 여백 */
        }
    
        /* 어두운 색 버튼에 대한 스타일 */
	@@ -195,13 +190,42 @@
        .btn-block {
            width: 150px;
        }
        /*더보기 기능*/
        .text-wrapper {
            width : 475px;
        }
        .text {
            /* 요소의 내용이 영역을 벗어날 경우 그 부분은 숨겨지게 처리 */ 
            overflow: hidden;
            /* 요소의 표시 방법을 -webkit-box로 표시 */
            display: -webkit-box;
            /* webkit-box로 표시된 자식 요소들의 배치 방법 결정 (여기서는 수직) */ 
            -webkit-box-orient: vertical;
            /* 요소 내 텍스트를 지정한 라인수까지만 표시 */
            -webkit-line-clamp: 13;
            /* 텍스트 양쪽정렬 */ 
            text-align :justify;
        }
        
        .more-text {
            /* 글씨 크기,색상 지정 */ 
            font-size: 16px;
            color:pink;
        
            /* 마우스 포인트가 올라갔을 때 커서 모양을 포인터로 변경 */
            cursor: pointer;
        
        }
        
        .less-text {
        
            /* 글씨 크기,색상 지정 */
            font-size: 16px;
            color:blue;
            /* 마우스 포인트가 올라갔을 때 커서 모양을 포인터로 변경 */ 
            cursor: pointer;
            /* 줄이기 버튼 (처음에는 안보이게) */
            display: none;    
        
        }
    </style>