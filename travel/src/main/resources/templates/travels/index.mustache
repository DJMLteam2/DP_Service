{{>layouts/header}}

<table class="table">
    <thread>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Location</th>
            <th scope="col">Latitude</th>
            <th scope="col">Longitude</th>
        </tr>
    </thread>
    <tbody>
        {{#travelsList}}
        <tr>
            <th>{{id}}</th>
            <th>{{location}}</th>
            <th>{{latitude}}</th>
            <th>{{longitude}}</th>
        </tr>
        {{/travelsList}}
    </tbody>
</table>

{{>layouts/footer}}